name: Update BOSS IPTV M3U

on:
  schedule:
    - cron: '0 */3 * * *'  # Her 3 saatte bir
  workflow_dispatch:

jobs:
  update-m3u:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Depoyu klonla
      uses: actions/checkout@v4

    - name: Python 3.11 kurulumu
      uses: actions/setup-python@v5
      with:
        python-version: 3.11

    - name: Gerekli Python kütüphaneleri
      run: pip install -U requests

    - name: boss.py çalıştır
      run: python boss.py

    - name: Değişiklik kontrolü
      id: degisiklik
      run: |
        git diff --quiet . || echo "degisiklik=true" >> $GITHUB_OUTPUT

    - name: Değişiklikleri commit ve push et
      if: steps.degisiklik.outputs.degisiklik == 'true'
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add boss-iptv.m3u
        git commit -m "🔄 BOSS IPTV M3U güncellendi - $GITHUB_SHA"
        git push
