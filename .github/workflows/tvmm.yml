name: popcorrn Güncelleme

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

concurrency:
  group: popcorrn-guncelleme-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Repo'yu klonla
        uses: actions/checkout@v4

      - name: Playlist'i indir (hatasız mod)
        run: |
          echo "📥 Playlist indiriliyor..."

          # curl HİÇBİR ŞEKİLDE workflow'u düşürmez
          curl -L \
            --connect-timeout 15 \
            --max-time 60 \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" \
            -o indirildi_playlist.m3u \
            "http://ns3051909.ip-137-74-92.eu:25461/get.php?username=900EB311156D&password=PopcornTV&type=m3u_plus&output=m3u" \
            || true

          # Dosya yoksa
          if [ ! -f indirildi_playlist.m3u ]; then
            echo "❌ Dosya oluşmadı (erişim engelli olabilir)"
            exit 0
          fi

          # Dosya boşsa
          if [ ! -s indirildi_playlist.m3u ]; then
            echo "❌ Dosya boş, siliniyor"
            rm -f indirildi_playlist.m3u
            exit 0
          fi

          # M3U kontrolü
          if ! head -n 1 indirildi_playlist.m3u | grep -q "#EXTM3U"; then
            echo "❌ Geçersiz M3U içeriği"
            rm -f indirildi_playlist.m3u
            exit 0
          fi

          echo "✅ Playlist başarıyla indirildi"

      - name: Commit ve Push
        run: |
          if [ ! -f indirildi_playlist.m3u ]; then
            echo "ℹ️ Commit edilecek dosya yok"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add indirildi_playlist.m3u

          if git diff --cached --quiet; then
            echo "ℹ️ Değişiklik yok"
            exit 0
          fi

          git commit -m "Otomatik Güncelleme: Popcorn M3U"
          git pull --rebase origin main
          git push origin main

          echo "🚀 Güncelleme gönderildi"
