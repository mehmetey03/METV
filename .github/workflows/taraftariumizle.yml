name: Taraftariumizle M3U8 Updater

permissions:
  contents: write   # Push izni için gerekli

on:
  schedule:
    - cron: '0 */2 * * *'  # Her 2 saatte bir çalış
  workflow_dispatch:        # Manuel çalıştırma için
  push:
    branches: [ main ]
    paths-ignore:          # Bu dosyalar değişince tetiklenmesin
      - '*.m3u'
      - '*.json'

jobs:
  update-files:
    runs-on: ubuntu-latest
    timeout-minutes: 15    # Maksimum çalışma süresi

    steps:
      - name: 🛒 Repository'yi checkout et
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # Tüm commit geçmişini al
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Python kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 📦 Gerekli kütüphaneleri kur
        run: |
          python -m pip install --upgrade pip
          pip install playwright beautifulsoup4
          playwright install chromium
          playwright install-deps

      - name: 🚀 Python scriptini çalıştır
        run: |
          echo "Python scripti başlatılıyor..."
          python tarafmi.py
          echo "Script tamamlandı"
          
          # Oluşan dosyaları kontrol et
          ls -la *.m3u *.json 2>/dev/null || echo "Dosya bulunamadı"

      - name: 📊 Değişiklikleri kontrol et
        id: check-changes
        run: |
          git status
          if git diff --quiet; then
            echo "No changes to commit"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected"
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

      - name: 📝 Değişiklikleri commit et
        if: steps.check-changes.outputs.has_changes == 'true'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "🤖 Auto-update: $(date +'%Y-%m-%d %H:%M:%S')"
          echo "✅ Commit oluşturuldu"

      - name: 🔄 Remote değişiklikleri çek
        if: steps.check-changes.outputs.has_changes == 'true'
        run: |
          # Uzak değişiklikleri çek ve rebase yap
          git fetch origin main
          if git rebase origin/main; then
            echo "✅ Rebase başarılı"
          else
            echo "⚠️  Rebase başarısız, merge denenecek"
            git rebase --abort
            git merge origin/main --no-edit || git merge --abort
          fi

      - name: 🚀 Değişiklikleri push et
        if: steps.check-changes.outputs.has_changes == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Force push yerine normal push yap
          git push origin HEAD:main
          echo "✅ Push tamamlandı"

      - name: 📋 Workflow özeti
        if: always()
        run: |
          echo "=== Workflow Tamamlandı ==="
          echo "Zaman: $(date)"
          echo "Çalışma: ${{ job.status }}"
          
          # Dosya boyutlarını göster
          if [ -f "taraftariumizle.m3u" ]; then
            echo "M3U dosya boyutu: $(wc -l < taraftariumizle.m3u) satır"
          fi
